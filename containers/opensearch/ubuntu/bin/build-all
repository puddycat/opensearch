#! /usr/bin/env bash
#
# opensearch/containers/opensearch/ubuntu/bin/build-all
#
# raymondstrose@hotmail.com
#
#   Build Opensearch Docker images.
#

build_versions ()
{
	cat <<-EOF
		3.0.0
		EOF
}

for name in $(build_versions); do
	docker system prune --all --force;
	bin/build --target-image-tag=$name;
done;
